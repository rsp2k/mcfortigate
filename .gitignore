# Python
__pycache__/
*.py[cod]
*.so
build/
dist/
*.egg-info/
.eggs/

# Virtual environments
.venv/
venv/

# Tooling caches
.ruff_cache/
.pytest_cache/
.coverage
htmlcov/

# uv
uv.lock

# Credentials and local config
.env
.env.local
.mcp.json

# Operator notes. Holds live lab and infrastructure detail — dell01's interface
# layout, the lab segment, MACs, the production gateway — and this repo now has
# a public GitHub remote alongside the private Gitea one. It has never been
# tracked; this keeps a stray `git add -A` from changing that.
CLAUDE.md

# Local scratch
/local/
/artifacts/
